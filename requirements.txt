# Site build toolchain. Install into a virtualenv:
#   python3 -m pip install -r requirements.txt
jupyter-book>=2.1,<3
jupyterlite-core>=0.8,<0.9
jupyterlite-pyodide-kernel>=0.8,<0.9

# Needed only to run the notebooks locally before committing their outputs.
# Not needed by CI (the build does not execute anything) and not needed by
# JupyterLite (Pyodide ships its own numpy/matplotlib).
numpy
matplotlib
nbconvert
